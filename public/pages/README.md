Drop your project screenshots/images here (jpg, png, etc).

Then in src/main.js, reference one like this in a page entry:
  { image: "/pages/your-filename.jpg" }

The leading slash matters — it tells the browser to look at the site root, not relative to wherever main.js is.
